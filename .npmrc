# Use sistema CA (resolve problema TLS no Windows com antivirus)
use-system-ca=true

# Desativar strict-ssl como fallback em redes com proxy/interceptor TLS
strict-ssl=false

# Auto-instala peer dependencies (evita warnings)
auto-install-peers=true

# Strict peer deps
strict-peer-dependencies=false
