{
  "name": "@vectorgov-t/ingestion-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Worker wrapper para o Container Python do LegisParser",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "echo 'tests are pytest, see tests/'"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251201.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.59.0"
  }
}
