# Vectorgov-t - Ingestion API (Container Python)
# Parser de legislacao brasileira + endpoint HTTP

# HTTP framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.18

# Validacao e schemas
pydantic>=2.9.0

# Parsing de PDFs (deterministico, sem OCR)
PyMuPDF>=1.24.0

# Dev / Tests
pytest>=8.3.0
pytest-asyncio>=0.24.0
httpx>=0.28.0  # usado pelo TestClient do FastAPI
