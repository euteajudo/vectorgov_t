{
  "//": "TODO(merge Track H): este tsconfig foi criado pela Track I (F3.I.1) apenas para o typecheck local compilar JSX. A Track H provavelmente vai sobrescrever este arquivo com a config completa do Next.js (paths @/, plugin next, jsx preserve etc.). Manter compat com o monorepo: estender '../../tsconfig.json' e preservar 'jsx', 'lib' DOM e os paths '@vectorgov-t/*'.",
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "jsx": "preserve",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "moduleResolution": "Bundler",
    "module": "ESNext",
    "target": "ES2022",
    "types": ["node"],
    "allowJs": true,
    "incremental": true
  },
  "include": ["src/**/*", "next-env.d.ts"],
  "exclude": ["node_modules", ".next", "out"]
}
